# pytest.ini
[pytest]
markers =
    playwright: marks tests as playwright tests
    slow: slow tests
    integration: integration tests
    unit: unit tests
filterwarnings =
    ignore::DeprecationWarning:flask_mysqldb.*
    ignore::DeprecationWarning:reportlab.*
    ignore::DeprecationWarning:ast.*